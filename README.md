# desarrollo_web_bastian_valencia

Algunas consideraciones:

- El diálogo de agregar-aviso.html cuando el formulario está correcto no es exactamente el mismo que el especificado en el archivo, pero tiene las mismas interacciones
- Se decidió crear páginas estáticas para cada aviso y así facilitar el código